function findOdd(A) {
  let num;
  var times=0;//new Array(A.length);
  for(var i=0;i<A.length;i++)
  {
    num = A[i]
    for(var j=0;j<A.length;j++)
    {  
      if(num == A[j])
          ++times;
    }
    if(times%2 == 1)
      break;
  }
  return A[i];
}
